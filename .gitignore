# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# 虚拟环境
.venv/
venv/
env/

# IDE
.vs/
.vscode/
.idea/
*.suo
*.user

# 构建产物
build/
dist/
*.egg

# 环境变量 (含密钥, 禁止入仓, AGENTS.md 第 11 章)
.env
.env.agent
.env.prod
.env.dev
!.env.template

# 上传文件
packages/uploads/
uploads/

# 离线包 (体积大, 按需下载, 不入仓)
packages/wheels/
packages/debs/
packages/models/
packages/images/
!packages/wheels/.gitkeep
!packages/debs/.gitkeep
!packages/models/.gitkeep
!packages/images/.gitkeep

# QA 模式构建文件 (AGENTS.md 第 12 章三套构建模式, 不入仓)
docker-compose-qa.yaml
Dockerfile.qa
docker-build.qa.bat
scripts/build-offline.ps1
scripts/offline-deploy.ps1

# 生产离线模式构建文件 (AGENTS.md 第 12 章三套构建模式, 不入仓)
docker-compose-offline.yaml
Dockerfile.offline
docker-build.offline.sh

# 临时文件
*.log
*.tmp
*.bak
.DS_Store
Thumbs.db

# 数据
data/
sessions/
/docs
/.env.qa
/test_results
